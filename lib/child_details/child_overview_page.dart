import 'package:flutter/material.dart';

class ChildOverviewPage extends StatelessWidget {
  const ChildOverviewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Overview'),
    );
  }
}
